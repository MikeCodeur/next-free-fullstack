# Dashboard

### 💡 Faire un Dashboard protégé

## 📝 Tes notes

Détaille ce que tu as appris ici, sur une page [Notion](https://go.mikecodeur.com/course-notes-template)

## Comprendre

Maintenant que nous avons l’authentification, **nous** pouvons protéger des routes de la manière suivante. 

```tsx
 const user = await auth()
  if (!user) {
    redirect('/login')
  }
```

Nous allons maintenant pouvoir développer ici la partie fonctionnelle de notre `SaaS`.

## Exercice

Utilise la `sidebar` de `shadcn` et intègre dans un `Layout`

[https://ui.shadcn.com/blocks](https://ui.shadcn.com/blocks)

```tsx
pnpm dlx shadcn@latest add sidebar-03
```

Fichiers

- `app/dashboard/layout`

## Bonus

### 1. 🚀 Utilise [V0.dev](http://V0.dev) pour générer un Dashboard

https://v0.dev/

Récupère ce `dashboard` dans ton projet avec `ShadCn cli`

Fichiers

- `app/dashboard/page`

### 2. 🚀 Créer une nouvelle route

Crée une `feature` de ton choix 

Fichiers

- `app/dashboard/route/page`

## Aller plus loin

📑 Le lien vers la doc [https://v0.dev/](https://v0.dev/)

## Ils vont t’aider

- **🐶  Mowgli le Chien** : *Mowgli te guidera dans chaque exercice.*
- **🤖  Ash le Robot** : *Ash le Robot te donnera du code utile.*
- **🚀 Julia La roquette** : *Julia te donnera des défis supplémentaires.*
- **⛏️ Hulk le Marteau** : *Quand du code à supprimer est présent*
- **👨‍✈️ Hugo le chef de projet** : *Va t'aider sur les spécifications du projet*