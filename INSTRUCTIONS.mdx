# RSC : Appeler une base de données coté backend

### 💡 Interaction BDD avec React

## 📝 Tes notes

Detaille ce que tu as appris ici, sur une page [Notion](https://go.mikecodeur.com/course-notes-template)

## Comprendre

Nous avons vu que les **`React Server Components`** s'exécutent côté serveur (backend). Cela signifie que nous pouvons directement interagir avec une base de données depuis nos composants `React`, sans avoir à passer par une `API` intermédiaire.

Dans cet exercice, nous allons justement exploiter cette capacité. Pour simplifier la gestion des données, nous utiliserons **`LowDB`**, une base de données légère qui stocke les informations sous forme de fichier **`JSON`** sur le serveur. `LowDB` inclut déjà les fonctions `CRUD` nécessaires pour manipuler la table `user`, ce qui nous fera gagner du temps.

De base, nous avons 2 users : `user@gmail.com` et `admin@gmail.com`

📑 Le lien vers la doc lowDb https://github.com/typicode/lowdb

## Exercice

Dans cet exercice, tu vas devoir appeler `getUserByEmail('user@gmail.com')` dans un composant `React`.

Fichiers

- `app/rsc-bdd/page`

## Aller plus loin

📑 Le lien vers la doc [https://www.w3schools.com/html/html_css.asp](https://www.w3schools.com/html/html_css.asp)

## Ils vont t’aider

- **🐶  Mowgli le Chien** : *Mowgli te guidera dans chaque exercice.*
- **🤖  Ash le Robot** : *Ash le Robot te donnera du code utile.*
- **🚀 Julia La roquette** : *Julia te donnera des défis supplémentaires.*
- **⛏️ Hulk le Marteau** : *Quand du code à supprimer est présent*
- **👨‍✈️ Hugo le chef de projet** : *Va t'aider sur les spécifications du projet*